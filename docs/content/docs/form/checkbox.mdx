---
title: Checkbox
description: A control that allows the user to toggle between checked and not checked.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.checkbox/
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import { UsageSnippet } from '@/components/usage-snippet/usage-snippet';
import defaultSnippet from '@/snippets/examples/checkbox/default.json';
import disabledSnippet from '@/snippets/examples/checkbox/disabled.json';
import errorSnippet from '@/snippets/examples/checkbox/error.json';
import rawSnippet from '@/snippets/examples/checkbox/raw.json';
import formSnippet from '@/snippets/examples/checkbox/form.json';
import checkboxUsage from '@/snippets/usages/widgets/checkbox/checkbox.json';

<Callout type="info">
    For touch devices, a [switch](/docs/form/switch) is generally recommended over this.
</Callout>

<Callout type="info">
    We recommend using a [select group](/docs/form/select-group#checkbox-form) to create a group of checkboxes.
</Callout>

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='checkbox' query={{enabled: 'true'}}/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={defaultSnippet} />
    </Tab>

</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create checkbox
```

## Usage

### `FCheckbox(...)`
<UsageSnippet usage={checkboxUsage} />

## Examples

### Disabled

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='checkbox' query={{initialValue: 'true', enabled: 'false'}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={disabledSnippet} />
  </Tab>
</Tabs>

### Error

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='checkbox' query={{ error: 'Please accept the terms and conditions.' }}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={errorSnippet} />
  </Tab>
</Tabs>

### Without Label

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='checkbox' variant='raw' query={{ }}/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={rawSnippet} />
    </Tab>

</Tabs>

### Form

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='checkbox' variant='form' query={{}} height={550}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={formSnippet} />
  </Tab>
</Tabs>
