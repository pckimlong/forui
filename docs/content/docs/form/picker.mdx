---
title: Picker
description: A generic picker that allows an item to be selected. It is composed of one or more wheels, optionally with separators between those wheels. The picker supports arrow key navigation. Recommended for touch devices.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.picker/
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import { UsageSnippet } from '@/components/usage-snippet/usage-snippet';
import pickerUsage from '@/snippets/usages/widgets/picker/picker.json';
import pickerWheelUsage from '@/snippets/usages/widgets/picker/pickerWheel.json';
import pickerWheelBuilderUsage from '@/snippets/usages/widgets/picker/pickerWheelBuilder.json';
import defaultSnippet from '@/snippets/examples/picker/default.json';
import loopSnippet from '@/snippets/examples/picker/loop.json';
import builderSnippet from '@/snippets/examples/picker/builder.json';
import multipleSnippet from '@/snippets/examples/picker/multiple.json';
import separatorSnippet from '@/snippets/examples/picker/separator.json';

The picker supports arrow key navigation:

- Up/Down arrows: Increment/decrement selected value
- Left/Right arrows: Move between wheels

Recommended for touch devices.

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='picker' query={{}}/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={defaultSnippet} />
    </Tab>
</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create picker
```

## Usage

### `FPicker(...)`
<UsageSnippet usage={pickerUsage} />

### `FPickerWheel(...)`
<UsageSnippet usage={pickerWheelUsage} />

### `FPickerWheel.builder(...)`
<UsageSnippet usage={pickerWheelBuilderUsage} />

## Examples

### Loop

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='picker' query={{loop: 'true'}}/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={loopSnippet} />
    </Tab>
</Tabs>

### Lazy

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='picker' query={{builder: 'true'}}/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={builderSnippet} />
    </Tab>
</Tabs>

### Multiple Wheels

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='picker' variant='multiple'/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={multipleSnippet} />
    </Tab>
</Tabs>

### With Separators

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='picker' variant='separator'/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={separatorSnippet} />
    </Tab>
</Tabs>
