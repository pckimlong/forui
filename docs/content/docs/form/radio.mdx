---
title: Radio
description: A radio button that typically allows the user to choose only one of a predefined set of options.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.radio/
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import { UsageSnippet } from '@/components/usage-snippet/usage-snippet';
import defaultSnippet from '@/snippets/examples/radio/default.json';
import radioUsage from '@/snippets/usages/widgets/radio/radio.json';

<Callout type="info">
    We recommend using a [select group](/docs/form/select-group#radio-form) to create a group of radio buttons instead of using `FRadio` directly.
</Callout>

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='radio' variant='default' query={{}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={defaultSnippet} />
  </Tab>
</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create radio
```

## Usage

### `FRadio(...)`
<UsageSnippet usage={radioUsage} />
