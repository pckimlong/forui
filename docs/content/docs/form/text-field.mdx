---
title: Text Field
description: A text field lets the user enter text, either with a hardware keyboard or with an onscreen keyboard.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.text_field/
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import defaultSnippet from '@/snippets/examples/text-field/default.json';
import disabledSnippet from '@/snippets/examples/text-field/disabled.json';
import clearableSnippet from '@/snippets/examples/text-field/clearable.json';
import emailSnippet from '@/snippets/examples/text-field/email.json';
import passwordSnippet from '@/snippets/examples/text-field/password.json';
import multilineSnippet from '@/snippets/examples/text-field/multiline.json';

<Callout type="info">
  See [`FTextFormField`](/docs/form/text-form-field) for using text fields in forms.
</Callout>

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='text-field' query={{enabled: 'true'}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={defaultSnippet} />
  </Tab>
</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create text-field
```

## Usage

### `FTextField(...)`

```dart copy
FTextField(
  control: FTextFieldControl.managed(initial: TextEditingValue(text: '...')),
  style: (style) => style.copyWith(...),
  label: const Text('Email'),
  hint: 'john@doe.com',
  description: const Text('Enter your email associated with your Forui account.'),
  error: const Text('Error'),
  keyboardType: TextInputType.emailAddress,
  textCapitalization: TextCapitalization.none,
  enabled: true,
  clearable: (value) => value.text.isNotEmpty,
  maxLines: 1,
);
```

## Examples

### States

#### Enabled

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='text-field' query={{enabled: 'true'}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={defaultSnippet} />
  </Tab>
</Tabs>

#### Disabled

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='text-field' query={{enabled: 'false'}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={disabledSnippet} />
  </Tab>
</Tabs>

### Clearable

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='text-field' variant='clearable' query={{}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={clearableSnippet} />
  </Tab>
</Tabs>

### Presets

#### Email

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='text-field' variant='email' query={{}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={emailSnippet} />
  </Tab>
</Tabs>

#### Password

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='text-field' variant='password' query={{}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={passwordSnippet} />
  </Tab>
</Tabs>

#### Multiline

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='text-field' variant='multiline' query={{}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={multilineSnippet} />
  </Tab>
</Tabs>
