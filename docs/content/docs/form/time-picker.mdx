---
title: Time Picker
description: A time picker that allows a time to be selected. The picker supports arrow key navigation. Recommended for touch devices.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.time_picker/
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import { UsageSnippet } from '@/components/usage-snippet/usage-snippet';
import defaultSnippet from '@/snippets/examples/time-picker/default.json';
import hour24Snippet from '@/snippets/examples/time-picker/hour24.json';
import intervalSnippet from '@/snippets/examples/time-picker/interval.json';
import animatedSnippet from '@/snippets/examples/time-picker/animated.json';
import timePickerUsage from '@/snippets/usages/widgets/time_picker/timePicker.json';

The picker supports arrow key navigation:

- Up/Down arrows: Increment/decrement selected value
- Left/Right arrows: Move between wheels

Recommended for touch devices.

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='time-picker' query={{}}/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={defaultSnippet} />
    </Tab>
</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create time-picker
```

## Usage

### `FTimePicker(...)`
<UsageSnippet usage={timePickerUsage} />

## Examples

### 24 Hour Clock

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='time-picker' query={{hour24: 'true'}}/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={hour24Snippet} />
    </Tab>

</Tabs>

### With Custom Intervals

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='time-picker' variant='interval'/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={intervalSnippet} />
    </Tab>

</Tabs>

### Animated

<Tabs items={['Preview', 'Code']}>
    <Tab value="Preview">
        <Widget name='time-picker' variant='animated' height={500}/>
    </Tab>
    <Tab value="Code">
        <CodeSnippet snippet={animatedSnippet} />
    </Tab>

</Tabs>
