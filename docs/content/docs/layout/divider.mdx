---
title: Divider
description: Visually or semantically separates content.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.divider/
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import defaultSnippet from '@/snippets/examples/divider/default.json';

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='divider' query={{}} height={300}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={defaultSnippet} />
  </Tab>
</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create dividers
```

## Usage

### `FDivider(...)`

```dart copy
FDivider(
  style: (style) => style.copyWith(...),
  axis: Axis.vertical,
);
```
