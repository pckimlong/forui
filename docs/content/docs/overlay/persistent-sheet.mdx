---
title: Persistent Sheet
description: A persistent sheet is displayed above another widget while still allowing users to interact with the widget below.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.sheet/
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import defaultSnippet from '@/snippets/examples/persistent-sheet/default.json';
import draggableSnippet from '@/snippets/examples/persistent-sheet/draggable.json';
import keepAliveOffstageSnippet from '@/snippets/examples/persistent-sheet/keep-alive-offstage.json';

It is part of [FScaffold](/docs/layout/scaffold), which should be preferred in most cases.

<Callout type="info">
    A closely related widget is a [modal sheet](/docs/overlay/sheet) which prevents the user from interacting with the
    rest of the app.
</Callout>

<Callout type="warn">
    All calls to `showFPersistentSheet(...)` should be made inside widgets that have either `FScaffold` or `FSheets` as
    their ancestor.
</Callout>

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='persistent-sheet' query={{}} height={500}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={defaultSnippet} />
  </Tab>
</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create persistent-sheet
```

## Usage

### `showFPersistentSheet(...)`

```dart copy
showFPersistentSheet(
  context: context,
  side: FLayout.ltr,
  style: (style) => style.copyWith(...),
  mainAxisMaxRatio: 9 / 16,
  constraints: const BoxConstraints(maxWidth: 450, maxHeight: 450),
  draggable: true,
  anchorPoint: Offset.zero,
  useSafeArea: false,
  resizeToAvoidBottomInset: true,
  keepAliveOffstage: false,
  onClosing: () {},
  builder: (context, controller) => const Placeholder(),
);
```

## Examples

### With `KeepAliveOffstage`

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='persistent-sheet' query={{keepAliveOffstage: true}} height={500}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={keepAliveOffstageSnippet} />
  </Tab>
</Tabs>

### With `DraggableScrollableSheet`

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='persistent-sheet' query={{}} height={400} variant='draggable'/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={draggableSnippet} />
  </Tab>
</Tabs>
