---
title: Sheet
description: A modal sheet is an alternative to a menu or a dialog and prevents the user from interacting with the rest of the app.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.sheet/
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import { UsageSnippet } from '@/components/usage-snippet/usage-snippet';
import defaultSnippet from '@/snippets/examples/modal-sheet/default.json';
import blurredSnippet from '@/snippets/examples/modal-sheet/blurred.json';
import draggableSnippet from '@/snippets/examples/modal-sheet/draggable.json';
import sheetShowUsage from '@/snippets/usages/widgets/modal_sheet/show.json';
import sheetRouteUsage from '@/snippets/usages/widgets/modal_sheet/route.json';

It navigates to a new page each time.

<Callout type="info">
    A closely related widget is a [persistent sheet](/docs/overlay/persistent-sheet), which shows information that
    supplements the primary content of the app without preventing the user from interacting with the app.
</Callout>

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='modal-sheet' query={{}} height={500}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={defaultSnippet} />
  </Tab>
</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create modal-sheet
```

## Usage

### `showFSheet(...)`
<UsageSnippet usage={sheetShowUsage} />

### `FModalSheetRoute(...)`
<UsageSnippet usage={sheetRouteUsage} />

## Examples

### Blurred Barrier

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='modal-sheet' variant='blurred' query={{}} height={500}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={blurredSnippet} />
  </Tab>
</Tabs>

### With `DraggableScrollableSheet`

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='modal-sheet' query={{}} height={400} variant='draggable'/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={draggableSnippet} />
  </Tab>
</Tabs>
