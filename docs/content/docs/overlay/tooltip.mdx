---
title: Tooltip
description: A tooltip displays information related to a widget when focused, hovered over, or long pressed.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.tooltip/
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import defaultSnippet from '@/snippets/examples/tooltip/default.json';
import horizontalSnippet from '@/snippets/examples/tooltip/horizontal.json';
import longPressOnlySnippet from '@/snippets/examples/tooltip/long-press-only.json';

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='tooltip' height={300}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={defaultSnippet} />
  </Tab>
</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create tooltip
```

## Usage

### `FTooltip(...)`

```dart copy
FTooltip(
  control: FTooltipControl.managed(),
  style: (style) => style.copyWith(...),
  tipAnchor: Alignment.bottomCenter,
  childAnchor: Alignment.topCenter,
  spacing: FPortalSpacing.spacing(4),
  overflow: FPortalOverflow.flip,
  hover: true,
  hoverEnterDuration: Duration(milliseconds: 500),
  hoverExitDuration: Duration.zero,
  longPress: true,
  longPressExitDuration: Duration(milliseconds: 1500),
  tipBuilder: (context, controller) => const Text('Tooltip'),
  builder: (context, controller, child) => child!,
  child: const Placeholder(),
);
```

## Examples

### Horizontal Alignment

You can change how the tooltip is aligned to the button.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='tooltip' query={{axis: 'horizontal'}} height={300}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={horizontalSnippet} />
  </Tab>
</Tabs>

### Long Press Only

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='tooltip' query={{hover: 'false'}} height={300}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={longPressOnlySnippet} />
  </Tab>
</Tabs>
