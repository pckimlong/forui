---
title: Tile
description: A specialized Item for touch devices, typically used to group related information together.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.tile/
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import tileDefault from '@/snippets/examples/tile/default.json';
import tileUntappable from '@/snippets/examples/tile/untappable.json';
import tileDisabled from '@/snippets/examples/tile/disabled.json';
import tileSubtitle from '@/snippets/examples/tile/subtitle.json';
import tileDetails from '@/snippets/examples/tile/details.json';

<Callout type="info">
    Tiles can be grouped together inside a [FTileGroup](/docs/tile/tile-group).
</Callout>

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='tile' variant='default' query={{}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={tileDefault} />
  </Tab>
</Tabs>

## CLI

To generate and customize this style:

```shell copy
dart run forui style create tile
```

## Usage

### `FTile(...)`

```dart copy
FTile(
  title: const Text('Title'),
  style: (style) => style.copyWith(...),
  enabled: true,
  selected: false,
  semanticsLabel: 'Label',
  autofocus: false,
  focusNode: FocusNode(),
  onFocusChange: (focused) {},
  onHoverChange: (hovered) {},
  onStateChange: (delta) {},
  onPress: () {},
  onLongPress: () {},
  onSecondaryPress: () {},
  onSecondaryLongPress: () {},
  shortcuts: {SingleActivator(LogicalKeyboardKey.enter): ActivateIntent()},
  actions: {ActivateIntent: CallbackAction<ActivateIntent>(onInvoke: (_) {})},
  prefix: const Icon(FIcons.user),
  subtitle: const Text('Subtitle'),
  details: const Text('Details'),
  suffix: const Icon(FIcons.chevronRight),
);
```

### `FTile.raw(...)`

```dart copy
FTile.raw(
  child: const Text('Title'),
  style: (style) => style.copyWith(...),
  enabled: true,
  selected: false,
  semanticsLabel: 'Label',
  autofocus: false,
  focusNode: FocusNode(),
  onFocusChange: (focused) {},
  onHoverChange: (hovered) {},
  onStateChange: (delta) {},
  onPress: () {},
  onLongPress: () {},
  onSecondaryPress: () {},
  onSecondaryLongPress: () {},
  shortcuts: {SingleActivator(LogicalKeyboardKey.enter): ActivateIntent()},
  actions: {ActivateIntent: CallbackAction<ActivateIntent>(onInvoke: (_) {})},
  prefix: const Icon(FIcons.user),
);
```

## Examples

### Untappable

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='tile' variant='default' query={{tappable: false}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={tileUntappable} />
  </Tab>
</Tabs>

### Disabled

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='tile' variant='default' query={{enabled: false}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={tileDisabled} />
  </Tab>
</Tabs>

### With Subtitle

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='tile' variant='subtitle' query={{}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={tileSubtitle} />
  </Tab>
</Tabs>

### With Details

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='tile' variant='details' query={{}}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={tileDetails} />
  </Tab>
</Tabs>
